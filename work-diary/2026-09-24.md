# Work diary — 2026-09-24 (Thursday)

## Tasks due today

Carried over from [yesterday](2026-09-23.md): the design-system plan is built but
**unverified** — nothing in the viewer has been looked at since the chrome turned charcoal, the
opening view moved to the valley, or `?relief=thread` landed. Still nobody's eyes on it.

- [x] Review the four chapter plans against the engine and apply the architecture fixes
- [x] Copy pipeline: Google Doc → ArchieML → repo, links preserved, English only
- [ ] Verify the design system in the viewer — the carry-over that keeps carrying

<!-- Written FIRST THING, before any work. Do not edit it later to match what happened —
     the gap between this and the work log is the useful part.
     With a tracker: the issue table (ID · Title · Priority · Status · Plan · Link).
     Without one: the plan of attack as bullets, in priority order.
     Carrying work over? Link yesterday's entry and say what moved. -->

- [ ]

## Work log

- **Exhibition opens today.** No code goes near the machine; the day is plans and pipeline.

- Applied the architecture review to the four chapter plans (`48c1c5f`). The important ones:
  hotspots are the document's (the Futures plan had invented `pins.ts` beside a `Hotspot` type
  that already had `LocaleMap`), `ChapterId` exists because the rail was keyed by view and
  breaks the moment Futures enters the valley, and `registers.ts` is dismantled rather than
  renamed — `Diorama` still imported six things from it. Sized the column budget: 512² is
  534k triangles, 3.56× the phone ceiling, and the exhibition screen draws it anyway, which
  is now a recorded priority in `CLAUDE.md`.

- **Copy comes from a Google Doc.** Yan's decision; the doc exists with four empty tabs. The
  reference script in `malaysia-data-center-story` goes public-export → Turndown → ArchieML.
  Two things didn't carry: the doc is private (export URL 401s) and that script isn't
  tab-aware. `gws` is upstream of both problems — the Docs API hands over `link.url` per run
  and per-tab content, so links are preserved rather than reconstructed. `docToMarkdown` and
  `renderCopy` are pure and tested; the script is transport only; no credential in the repo.
  Byte-identical on re-run, proven. Bold and italic are dropped on purpose — a bold `key:`
  becomes `**key:**` and ArchieML reads prose. That is the exact bug the reference script
  regexes around.

- **The shell is scrollable.** Five foundations, five commits: `ChapterId` (and it fixed the
  number keys, which were still on the old scale order), the `Hotspot` extension with
  `validateCopyJoin`, `camera.ts` extended plus `CameraRig` — and `registers.ts` finally
  deleted, three days after CLAUDE.md said it had been; its crossfade watcher was defined and
  never mounted — `chapters.ts` with `beatAt`/`resolvePose`/`validateScore`, and then the
  visible layer: the viewer is the scroll container, the stage sticks, one section per beat
  scrolls over it with a centred card, the beat names the view and a pose relative to its fit,
  and the terminal beat flips `mode` to explore. Scores are committed content joined to the
  doc's copy by id, with a test that fails the build if either side drifts. Nobody has looked
  at it: Chrome will not screenshot `/`. What I could check — the server-rendered HTML carries
  the stem, the lit first card, the sample headline and the hyperlink as a real anchor; the
  console is clean across two loads; both routes build.

- **Seeded the doc.** Yan asked for sample copy in the Google Doc, so the four tabs now hold
  12 beats and 16 hotspots written from the research notes, with the ids the chapter context
  files use — the join to coordinates and icons is already honest. One real hyperlink per
  chapter, deliberately, so the round trip is demonstrable rather than asserted: all four came
  back as `[text](url)` inside parsed ArchieML values. Written with `gws docs documents
  batchUpdate`, one `insertText` per tab and an `updateTextStyle` with `link` per phrase,
  offsets computed from the segments. Byte-identical on re-run with real content.

- Wrote the test "never emits HTML" wrong the first time. Runs are data; `<script>` in copy is
  legitimate text that React escapes. The guarantee is the run's *shape* — `text` and an
  optional safe `href` — and now the test says so.

<!-- Chronological, written as you go. What was actually done, why, what it cost.
     Reference commits and link to plans and notes. Prose beats a checklist here —
     this is the part you will actually reread in three months. -->

-

- **Yan's eyes on the viewer, and three asks back** (`plans/2026-09-24_north-up-and-chrome`).
  The credits paragraph goes behind an `i` in the stage's corner (`Credits.tsx`, a native
  `<details>` with `Escape` added); the topbar loses its rule and its `EN`/`debug` buttons,
  the header becomes kicker-over-headline like p22, buttons become text, and the one
  filled control is the Explore bar's `Next →` in the invert register. The valley's town
  labels were printing over the circle and the city — `Html` ignores its group's
  `visible` — so `ValleyView` takes `labelled` the way `RegionPlane` takes `labels`. The
  Next dev indicator sat exactly on the credits button; `devIndicators: false`.

  **The camera: a wrong turn, and what it left behind.** I read "keep the orientation
  north to south" as north-up, built it — camera due south, 45° pitch, backdrop
  re-rendered, 583 tests — and Yan saw it and said no: "we want the same angle we had
  before". The angle is back on the diagonal. What survives is that the attitude now
  lives in ONE place, `camera.ts` (`CAMERA_YAW`, `CAMERA_PITCH`, `screenBasis`,
  `projectView`, `groundDepth`, `wallFacesCamera`, `rightness`), where before it was
  restated in six files; the backdrop fingerprint hashes it, so turning the camera fails
  `backdrop-freshness.test.ts` until `npm run render:backdrop`; and the circle's fit is
  computed from its own bounds rather than derived from the district's. The committed
  raster changed by a few bytes — a wall within 15° of the tone split now takes the lit
  tone — and the sidecar carries the new fingerprint.

  **What "orientation" meant is still open.** Yan's screenshot of the valley this morning
  shows a wide slab with its top edge tilted about 7°, which is neither the diagonal's
  rhombus nor anything the square 120 km field can produce at that camera. Chrome froze
  before I could measure the canvas in the stem; my one stem screenshot had a blank stage
  where the valley should have been. Browser verification is Yan's from here, by request.

- **Present, started** (`plans/2026-09-23_present-chapter`, now `active`). The three region
  artefacts regenerated on the scene origin with `build-region.py --centre 18.7912,99.0043`;
  the generator now writes a `curve` into each sidecar — cumulative population every 50 km —
  and `region.ts` reads it with `peopleWithin` and `halfPopulationRadius`. The new 3,437 km
  field holds 3.98 bn against 4.09 bn at the Valeriepieris centre: the move cost 2.6%, and the
  curve matches the plan's table (2,000 km → 1.61 bn, 3,000 km → 3.60 bn). `region.test.ts`
  fails if a field's centre differs from the scene origin, which is the test that catches a
  regeneration on the old centre.

  The field is columns now: `columns.ts` (pure — layout, a three-face unit box derived from
  the camera basis, log-scaled height) and `RegionColumns.tsx` (one `InstancedMesh`, ring
  radius as a uniform patched into `MeshBasicMaterial`, picking by instance id). A beat can
  carry `ring: { from, to }` in multiples of the claim radius; `ringAt` interpolates it with
  the beat's progress, and a ring beat also interpolates the camera toward the next beat's
  pose, so the world recedes as the circle grows. The counter under the card reads the curve.
  The explore caption says "about 3,400 km", rounded to the hundred, with the 7.8–8.2 bn
  bracket as the footnote — the plan's three open questions taken as: rounded with a footnote,
  the ring keeps growing past the claim in the muted tone, and the field's own 3,437 km rim
  stays drawn as the ghost of the old radius.

- **The columns, seen, and a change of renderer.** Yan's screenshot of the Present view at 07:52
  was black speckle — `vertexColors` on a geometry with no `color` attribute — and, under the
  bug, a forest of full-height needles. Fixed (instanceColor alone; 256² by block-sum; cube-root
  height) and pushed. Then the real judgement: it does not read because there is no geography
  under it — no coastline, no borders, no relief, no names. Yan asked whether MapLibre or Mapbox
  with PMTiles would be better; I said MapLibre, for a self-hosted basemap that works offline and
  a globe on which a 3,400 km circle is a circle; Yan said make the plan. It is
  `plans/2026-09-24_present-on-maplibre.plan.md`, `draft`, and it says what is retired and why.
  Facts it rests on: MapLibre GL JS 6.11 with globe + fill-extrusion, a Protomaps world extract
  at zoom 0–6 of about 35 MB, `tippecanoe` already on this machine, and `pmtiles` not yet.

- **Present on MapLibre, built** (`plans/2026-09-24_present-on-maplibre`, `active`). `pmtiles`
  from Homebrew; a Protomaps world extract to zoom 6 (45 MB, `public/basemap/`, committed —
  both the offline laptop and Vercel need it in place with no server); `scripts/build-cells.py`
  bins the same GHS-POP tiles into 0.25° cells within the world disc — 130,804 cells, 7.14 bn,
  the world field's own total — and tippecanoe cuts them into an 11.6 MB PMTiles layer. `h3`
  would not build here, so squares. `mapstyle.ts` builds the whole Flavor from tokens and drops
  every symbol layer — labels stay DOM markers from the city file — and `mapstyle.test.ts`
  asserts no raw colour survives. `PresentMap.tsx` is the globe: cells as `fill-extrusion`
  coloured by the ramp and receded outside the ring, the ring as a geodesic circle from
  `circleAround()` (which is just `aeqdInverse` around a circle), `jumpTo` per scroll tick on a
  ring beat and `easeTo` otherwise, MapLibre's own hands in the bowl with rotation off. The
  page keeps the diorama mounted and hidden through Present, and mounts the map only there.
  The three.js circle is gone: `RegionPlane`, `RegionColumns`, `columns.ts`, the circle branch of
  `Diorama`, and the circle's framing in `camera.ts` — one day old, and the cost of finding out.

  One determinism lesson: tippecanoe writes its own command line into the archive's metadata,
  input path included, so a `TemporaryDirectory` made every run differ. A fixed scratch path
  under `data/` and a fixed `-N` fixed it. The sidecar carries the file's SHA-256 and a test
  fails if the committed layer is not the one the script would write.

- **Seen, and three changes.** MapLibre's worker resolves relative to its own module URL,
  which Turbopack had moved — "Worker failed to load", blank map; the worker is copied to
  `public/maplibre/` on postinstall and `setWorkerUrl` points at it. My automation tab is
  backgrounded, so no animation frame runs and MapLibre never loads its style there: the
  look is Yan's. Yan's asks after looking: **flat, not globe** (Mercator; the ring is still
  true distance and bulges north); **the density of the old flat plane** — 13 km cells, so
  the 0.125° level now starts at zoom 4 with 0.25° and 0.5° only below, and column height
  scales with zoom so the fit reads as the density map; **no camera move in the stem** — the
  map sits at zoom 4, pitch 40, on Wat Ket, and only the ring grows. The cells pyramid is a
  block-sum of one 0.125° grid, coloured and heightened by density on one scale so a place
  keeps its colour across levels, and joined by `tile-join`.

- **Icons, in parallel** (`plans/2026-09-24_pin-icons`, `active`). Yan asked for the Higgsfield
  icon run to start while Present was still being built, and pointed at the revised exhibition
  copy in `docs/references/` as the brief. A style probe first — KAE on Nano Banana 2 with three
  KV images as references, Recraft with the palette pinned, GPT Image 2 — and Nano Banana won:
  it held the palette and the KV's paper-cut felt, where Recraft went flat and GPT Image 2 went
  terracotta at three times the price. Yan, mid-run: the style is right, but they have to be
  isometric — which the prompt already states as the renderer's own camera, 45° and 35°, and all
  three probes were. Then `scripts/icons/manifest.json` (sixteen subjects, one per hotspot id)
  and `scripts/gen-icons.py` (one style block, the refs, a labelled contact sheet). Round one is
  in `data/icons/` — gitignored; keepers go to `public/icons/` by hand. Fourteen of sixteen hold;
  Wat Umong came back on brick and Lamphun town on a white plate. Every one still stands on a
  thin diamond base the prompt asked it not to have, and whether a pin *wants* one is now a
  question for the diorama rather than the sheet. The basic plan allows four concurrent jobs
  and 503s twice in sixteen; the script reruns by id. 126 credits → 73.5, which is about ten
  more than the probe and sixteen images explain, and the job list does not show per-job cost.

- **New copy, from the references folder into the doc and back.** Yan dropped a revised
  draft (`wat-ket-exhibition-copy-gpt.md`, local only): a visitor's register rather than the
  team's research notes, the same twelve beat ids and sixteen hotspot ids, a new title —
  *Wat Ket and the world* — and one new field, `footnote`, on the Present `grow` beat. Written
  into the four tabs of the ArchieML doc with one `batchUpdate` (delete each tab's range, insert
  at 1; dry-run first), then `npm run fetch:copy`. Code: `footnote` on `BeatCopy`, rendered
  muted under the card; the page `<title>` reads the doc's title; the schema page documents the
  field. The footnote's bracket (3,303 / 3,416 / 3,524) is the plan's figures; the curve says
  3,301 / 3,412 / 3,527 and the explore caption computes its own, so the two differ by a few km.
- **The map's zoom is capped at 2.** Headless Chromium (Playwright, in the scratchpad) showed
  the cells vanish below zoom 2 — the archive has no tiles there — which is what Yan saw; it
  also showed all three levels rendering at their zooms and the ring growing with the counter,
  the first time this session saw the map at all. `docs/handover.md` should say: a hidden Chrome
  tab never loads MapLibre (throttled timers), and headless Chromium with SwiftShader does.

- **The icons are kept.** Yan, on the round-one grid: "I think we can keep these." No round two.
  `scripts/keep-icons.py` promotes the keepers named in `scripts/icons/keepers.json`: the white
  studio ground is keyed out by a flood fill from the frame edge — so cream walls inside the
  object survive — then trimmed, downscaled to 1024 on the long edge, and written as WebP with
  alpha to `public/icons/`, 2.2 MB for the set. A sidecar at `src/content/icons.json` records
  each sprite's size and its ground anchor, bottom-centre, and `src/engine/icons.ts` is the
  registry the shell plan wanted — `iconFor`, `validateIconJoin` — with a test pinning
  manifest, sidecar and files to each other. Byte-identical on re-run, checked twice. Two
  icons carry white ground the model painted into the picture, Wat Umong and Lamphun town,
  which shows on purple; kept as Yan said, one command to reroll. The first fill was a
  pixel-per-pass dilation and took half a minute an icon; vectorising the run fill helped less
  than expected because WebP at method 6 was 29 of the 35 seconds. Method 4 is 0.6 s for a
  file of the same size.

- **The chapter is in the hash, and the map is pre-warmed.** `#past`/`#present`/`#futures`,
  read on load and on hashchange, written with `replaceState` — and never over a valid hash
  until the visitor has switched, because React's development double-run of effects had the
  writer clobber `#present` before the second read. Then Yan: the map layers take too long to
  load. Measured headless: map mounted at 2.5 s, fully loaded at 30 s on SwiftShader, and a
  switch to Present afterwards shows 92k cells at once. So the map mounts hidden at page load
  and stays; the plan's tear-down is reversed, in `CLAUDE.md` and the plan.

- **The ring was re-parsing every tile per tick.** Yan: the expanding circle is extremely
  slow. Cause: each scroll tick called `setPaintProperty('fill-extrusion-color', …)` with the
  new radius, and MapLibre answers a change to a data-driven paint property by reloading the
  source's tiles through the worker — the whole first-load tessellation again, per tick. Fix:
  the colour expression is fixed and reads `feature-state`; `ringstate.ts` (pure, tested)
  finds by binary search the cells whose distance lies between the ring's last radius and its
  new one, and `RingStates` in `PresentMap.tsx` flips only those with `setFeatureState`. The
  archive is rebuilt with `--generate-ids` so every cell has a stable id; the index is rebuilt
  as tiles land. A tick now costs the cells it crosses, a few thousand, not the world.

- **The stem ends on a button.** Yan: the intro should not tip straight into explore, and the
  way back should be a real button, not a corner link. The terminal card now carries an
  "Explore the Past / Present / Futures" pill under it and only pressing it opens the bowl;
  `releasedAt` is no longer called by the page. In the bowl, "← Story" is gone from the bottom
  bar and "Read Introduction Again" sits top centre of the stage, returning to the first card
  rather than the last. `Escape` still skips to explore from the keyboard. Typecheck and 620
  tests green; browser check is Yan's.

- **Futures plan revised against the day.** Artwork and copy are done; nothing renders a hotspot
  and `wat-ket.json` has none. The plan now leads with a state table and a build order — data,
  `Pins.tsx` as drei `Html` buttons, popup, Chiang Dao clamped to the frame edge, a `City · Valley`
  pair in the explore bar, valley beats once the doc has them. Two cuts proposed for Yan: the story
  overlay (the newspaper is in the room) and edge arrows (one off-frame pin, Mae Chaem dropped with
  the copy). Valley stem proposed as three grouped beats, nine cards in all.

- **Yan decided the four futures questions** — popup at standfirst length with a prompt to the
  paper on the table instead of a story overlay; Chiang Dao clamped; three grouped valley beats;
  pins only when their beat arrives — and the landscape went in the same evening. Twelve
  hotspots in both scene documents with a `note` each; `pins.ts` pure (`pinsFor`, `placeInFrame`,
  `nearestPin`) and `PinLayer.tsx` as drei `Html` buttons with the popup inside the projected
  node; `City · Valley` in the explore bar; arrows walk the pins. `.renderer` is now
  `isolation: isolate` so drei's z-indexes stay under the chrome. **Named `PinLayer` because
  macOS cannot hold `Pins.tsx` beside `pins.ts`** — TypeScript resolved `./Pins` to the pure
  module. Nothing seen in a browser yet.

- **The Futures stem stays at four cards.** Yan: the doc's Future tab already has its copy. Fetched
  it (one word changed: "one *possible* Chiang Mai"); no valley beats are coming, so the valley is
  bowl material behind `City · Valley`, and the two city pins without beats ride the beats of their
  own Faiways piece — Wua Lai with Ban Tawan, Anusarn with the school. Camera pulled back to 2.5×
  and 3× so both pins of a pair are in frame.

- **The Futures stem crosses from the city to the valley.** Yan: "the scrolly itself will have
  transition between city and valley", and then "it should start with the city view" after a
  valley-first draft. With the doc's four beats that means one 2045 over the district, the two
  pin beats, then the weir card as the close over the whole valley with all eight regional pins —
  which moves `fai` after `lanna-world-school`, out of the doc's order; flagged to Yan. Two mechanics were missing: `CameraRig` tweened across a view change (600 ms of the valley
  at the city's zoom), now `moveDuration` cuts across views; and the city built on the scroll,
  now `Diorama` keeps the chapter's views warm. `availableChapters` became any-view and
  `defaultView` skips a missing one, so a scene with no valley still has Futures in the city.
  641 tests. Pushed the morning's two commits — the buttons Yan could not see were only ever local.

- **Six valley beats, from the doc.** Yan revised the local copy file with a "proposed valley
  extension" — `valley`, `doi-suthep`, `mae-taeng`, `doi-saket`, `lamphun-hills`, `kae` — and
  the file itself says to merge them into the live ArchieML. Inserted them into the Future tab with
  `gws docs documents batchUpdate` (insertText at the paragraph before the array's `[]`, with the
  tab id in the location), fetched, and scored them as the file's sweep: west, north, east, south,
  middle, revealing cumulatively and closing on the all-pins frame. The weir card is back in the
  doc's order, in the city. The `City · Valley` control became a pill toggle under the rail.

- **The valley dressed.** Purple in the hillshade's shadows — a two-ended ramp, Warm White to a
  slate-violet mix, replacing base × light; the lit end is unchanged so the floor still matches the
  diorama's ground. Rivers as fat lines (drei `Line` in `segments` mode, one draw per weight): the
  Ping 7 px, named rivers 3.5, reservoir outlines 1.25. `fetch-valley.ts` now also pulls
  motorway/trunk/primary and `railway=rail` — 2,105 road ways, 38 rail, rivers byte-identical to
  before — and the Futures shows them as slate roads and a dashed charcoal railway; the Past does
  not. A town label hides when a visible pin carries its name, per Yan on Lamphun.

- **Popups, second pass.** Found by reading rather than tapping: R3F connects its events to the
  div around the canvas, which is where drei mounts `Html`, so a tap on a card's text bubbled up
  as `onPointerMissed` and closed the card. Native `stopPropagation` on the pin wrapper — React's
  handlers run after the fiber's, so a synthetic stop is too late. Cards now slide in from the
  stage edges and open under the icon when the top is too close (`fitPopup`, pure), with a tail.

- **Yan on the browser: no popups, labels unreadable, and "you've rotated a view".** The popup:
  the native `stopPropagation` from the pass before also stopped React's own click — React listens
  at the root, above the pin — so nothing opened. Replaced by a target check in the Diorama's
  `onPointerMissed`: a miss that lands inside `.pin` is not a miss. Labels: every map label —
  town, pin, Present city — is now dark text in a white pill, one shared rule in globals.css.
  The rotation: nothing in the diff touches a camera, bearing or pitch; asked which view.

- **The rotation, found.** Yan's two screenshots: the diorama north-up after the Futures stem.
  Not the constants — the backdrop fingerprint still passes — but OrbitControls: `CameraRig` set
  the controls' target and called `update()`, which keeps the camera where it is and re-derives
  the orbit. Targets used to move a kilometre or two; the valley beats move them 45 km, and the
  cut back to the city carried a 30° swing (camera 28 km out, target 11.6 km over). `standFor`
  in camera.ts now places the camera for every target along the attitude, and the rig applies
  position as well as target. Two tests: the offset is the same vector for any target.

- **Present: three stops, and Southeast Asia named.** Yan: beats at 0, 2,000 and the claim, no
  6,000 km card. Rewrote the Present tab's beats in the doc (delete-range plus insert, by tab id),
  refetched; the ring is in kilometres now with `'claim'` as a named stop rather than multiples.
  Labels: a `focus` on `pickLabels` names every Southeast Asian city over a million, 120 km apart,
  before the sparse global pick — 38 labels instead of 20 — with Manila preferred over Quezon City
  and GeoNames' Budta and Malingao excluded. "Wat Ket, Chiang Mai" is a fixed label at the centre.

- **The Past, built on the Futures' machinery.** `threads.ts`: five threads with a `certainty`
  each and the grammar as code — confirmed solid, probable a broad dashed band, none nothing. The
  Ping alone for the river; two caravan corridors through documented towns to the frame edge;
  Highway 11 by `ref` (the fetch now keeps refs, 382 ways); the whole railway; the airport as a
  pin only. Station, tunnel and airport as `chapter: 'past'` hotspots from the findings'
  coordinates. In the stem each thread arrives with its card and the rivers are not substrate; in
  the bowl five pill toggles. No timeline scrubber — flagged in the plan as the open question.

## Plans & commits

<!-- generated: work-diary.py -->
### [Copy comes from a Google Doc, via ArchieML, with links intact](../plans/2026-09-24_copy-from-archieml.plan.md) · active

| Hash | Time | Subject |
|---|---|---|
| `c6cfb43` | 06:19 | feat: copy comes from a Google Doc, via ArchieML, links intact |
| `65dc6e7` | 06:24 | content: seed the copy doc with sample beats and hotspots |
| `032d97a` | 10:17 | content: the revised copy, a footnote field, and the piece's new title |

### [The scrolly shell, the icon system, and the popup/modal pair](../plans/2026-09-24_scrolly-shell.plan.md) · draft

| Hash | Time | Subject |
|---|---|---|
| `b06a75e` | 06:30 | feat: ChapterId — the rail, the keys and the tense move to chapters |
| `42e5575` | 06:32 | feat: a hotspot carries its chapter, and the copy join is checked |
| `d2ac6c1` | 06:40 | refactor: one camera, and registers.ts is finally deleted |
| `1d0d21f` | 06:43 | feat: chapters.ts — the beat score, pure and pinned |
| `ca4792f` | 06:51 | feat: the stem scrolls — cards over the scene, beats drive the camera |
| `3a3b961` | 10:31 | feat: the chapter is in the URL hash, so a refresh lands where you were |

### [North up, and chrome that belongs to the panels](../plans/2026-09-24_north-up-and-chrome.plan.md) · done

| Hash | Time | Subject |
|---|---|---|
| `85a3140` | 07:29 | refactor: the camera attitude is stated once, and the backdrop knows it |
| `d67c63d` | 07:30 | feat: credits behind an i, and chrome in the panels' language |
| `a59daa0` | 07:30 | docs: the morning's plan, its reversal, and where the attitude lives now |

### [Present — the circle grows out of Wat Ket](../plans/2026-09-23_present-chapter.plan.md) · active

| Hash | Time | Subject |
|---|---|---|
| `3a573c9` | 07:50 | feat: the circle is centred on Wat Ket, and the claim is computed |
| `abe1701` | 07:50 | feat: the population field as columns, and a ring that grows with the stem |
| `4640530` | 07:55 | fix: the columns had no colour, and were needles |

### [Present on MapLibre — a real map under the circle](../plans/2026-09-24_present-on-maplibre.plan.md) · active

| Hash | Time | Subject |
|---|---|---|
| `8da4dd4` | 07:58 | docs: plan the Present chapter's move to MapLibre and PMTiles |
| `c0a6bc3` | 09:10 | feat: a basemap and a population cells layer, as PMTiles |
| `cb32317` | 09:10 | feat: the Present chapter is a MapLibre globe, and the three.js circle goes |
| `853510d` | 09:17 | fix: serve MapLibre's worker from public, and surface its errors |
| `0066265` | 09:43 | feat: flat map, the field's own density, and a camera that holds still |
| `6cf8cbd` | 09:52 | fix: the zoom-scaled cell height is a valid MapLibre expression |
| `24f369d` | 10:17 | fix: the Present map zooms out no further than 3 |
| `20ed99b` | 10:34 | perf: the Present map mounts at page load, hidden, and stays warm |

### [Pin icons — sixteen isometric landmarks, generated on Higgsfield](../plans/2026-09-24_pin-icons.plan.md) · active

| Hash | Time | Subject |
|---|---|---|
| `6ac19bf` | 09:51 | feat: pin icons — manifest, generator and the first isometric round |
| `e23f826` | 10:25 | feat: the sixteen pin icons, kept, keyed and registered |

### Unplanned

| Hash | Time | Subject |
|---|---|---|
| `f875659` | 05:58 | docs: the scrolly shell, and three chapters built on it |
| `48c1c5f` | 06:07 | docs: apply the architecture review to the four chapter plans |
| `23a3cf0` | 06:19 | chore: record the copy pipeline in the diary |
| `d9baae3` | 06:24 | chore: record the doc seeding in the diary |
| `431b166` | 06:26 | docs: a handover for the next context refresh |
| `ed7ef0b` | 06:40 | chore: regenerate the diary's commit table |
| `2cb5f28` | 06:43 | chore: regenerate the diary's commit table |
| `27221b7` | 06:51 | chore: regenerate the diary's commit table |
| `1ba3462` | 06:53 | docs: handover updated — the shell is scrollable and unseen |
| `7ac2538` | 07:30 | chore: regenerate the diary's commit table |
| `f76f2bc` | 09:10 | chore: regenerate the diary's commit table |
| `32bed2a` | 09:11 | chore: ignore the cells build scratch |
| `bdb3996` | 09:43 | chore: regenerate the diary's commit table |
<!-- /generated -->

## Ad hoc todos

<!-- Things that surfaced today and are not in the tracker or a plan. Each gets a ### heading
     and a collapsed <details> block, so a future session can pick it up cold.
     Struck through with the resolution noted once fixed. -->

### The valley renders as a tilted slab in the stem

<details>
<summary>Yan's screenshot at 06:56 shows the valley as a wide slab, top edge tilted ~7°, in the Past stem. Neither the diagonal camera nor a north-up one produces that from a square field.</summary>

- Explore mode renders the valley correctly (seen at 07:15, north-up build).
- One stem screenshot (07:13) showed a blank stage with the first card over it; after `Escape` the
  valley appeared within two seconds.
- Hypothesis: in the stem the stage is `position: sticky; height: 100dvh` under a sticky topbar,
  and the canvas is measured by `useMeasuredStage` — if the buffer is sized at one layout and the
  CSS box settles at another, the render is stretched. Measure `canvas.width/height` against
  `getBoundingClientRect()` in the stem.
</details>

## Notes

<!-- Decisions made, things learned, blockers, what carries into tomorrow.
     Anything that changes the project's direction belongs in CLAUDE.md or the architecture
     docs as well — the diary is the trail, not the source of truth. -->

- **The camera stays on the diagonal.** Decided by Yan, 24 Sep 2026, after seeing a north-up
  build for a morning. The attitude is two constants in `camera.ts` and nothing else may restate
  it; the backdrop fingerprint carries it.
- **Credits behind an `i`; `EN` and `debug` off the bar; chrome in the panel language.** The
  locale button returns with Thai copy. Debug stays on `d`.
- **Browser verification is Yan's.** Said outright at 07:30. Build, test, curl the HTML, and
  hand over; do not drive Chrome at `/` — it freezes the renderer during the valley build anyway.
- **A misread instruction cost a morning.** "Orientation north to south" was not "north up".
  When an instruction about the picture is ambiguous, put the question to Yan before re-rendering
  a raster against a guess.
- **Icons are Nano Banana 2 with the KV as references, isometric at the renderer's own angles.**
  Yan's call on the style, 24 Sep 2026: the key visual's paper-cut relief, and it must be
  isometric. One style block in `scripts/gen-icons.py`; nothing per icon can touch it.
- **Present moves to MapLibre + PMTiles.** Yan's call, 24 Sep 2026, after seeing the columns.
  A second renderer, behind the cut between views; the data pipeline stays. Built the same
  day; verification on screen is Yan's. Not deadline-driven — Yan said so.
- **The basemap is committed.** 45 MB in `public/basemap/`. Git LFS stays rejected; this is a
  one-off binary the phone range-reads rather than downloads.
- Carry forward: review the MapLibre plan's open questions with Yan (extract zoom, squares or
  hexes), then start it; the tilted valley in the 06:56 screenshot did not reproduce.

